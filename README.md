# سازنده رزومه (CV Builder)

یک پروژه React + TypeScript + Vite برای ساخت رزومه با پیش‌نمایش زنده.

## ویژگی‌ها

- ✨ رابط کاربری مدرن با Tailwind CSS
- 🔄 پیش‌نمایش زنده رزومه
- 📦 ساختار کامپوننتی منظم و قابل توسعه
- ⚡ سریع و بهینه با Vite

## نصب و راه‌اندازی

```bash
# نصب وابستگی‌ها
npm install

# اجرای پروژه در حالت توسعه
npm run dev

# ساخت نسخه production
npm run build
```

## ساختار پروژه

```
src/
├── components/          # کامپوننت‌های قابل استفاده مجدد
│   ├── CVForm/         # فرم ورودی اطلاعات
│   ├── CVPreview/      # پیش‌نمایش رزومه
│   └── FormField/      # فیلد ورودی قابل استفاده مجدد
├── types/              # تعاریف TypeScript
└── App.tsx             # کامپوننت اصلی
```

---

# CV Builder

A React + TypeScript + Vite project for building a resume with live preview.

## Features

- ✨ Modern UI with Tailwind CSS
- 🔄 Live resume preview
- 📦 Clean, scalable component structure
- ⚡ Fast and optimized with Vite

## Install and Run

```bash
# Install dependencies
npm install

# Start dev server
npm run dev

# Build production bundle
npm run build
```

## Project Structure

```
src/
├── components/          # Reusable components
│   ├── CVForm/         # Input form
│   ├── CVPreview/      # Resume preview
│   └── FormField/      # Reusable input field
├── types/              # TypeScript types
└── App.tsx             # Main component
```
